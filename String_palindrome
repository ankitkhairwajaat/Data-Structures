#include<bits/stdc++.h>
using namespace std;
int main()
{
    string S;
    getline(cin,S);
    string P=S;
    bool flag;
    reverse(P.begin(),P.end());

    if(P==S)
    {
        flag=true;
    }
    else
    {
        flag=false;
    }
    
    cout<<flag;
    return 0;
}
